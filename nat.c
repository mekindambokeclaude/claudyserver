#include<stdio.h>
#include<stdlib.h>
int main(void)
{
    int a=0,b=0,c=0;
    printf("entrz les 03 valeurs\n",a,b,c);
    scanf("%d%d%d",&a&b&c); 
    printf("voici les valeurs entrees au clavier:/");
    printf("%d,%d,%d" || ,a,b,c);
    {
        if (b>=c)
        {
            printf("%d,%d,%d", a,b,c);
        }
        if(b>=a && a>=c || b>=a && c>=a) 
        if (>=)
    }
    return 0;
}
